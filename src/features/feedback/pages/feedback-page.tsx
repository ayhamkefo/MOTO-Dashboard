import { FeaturePlaceholder } from '../../../shared/components/feature-placeholder'

export function FeedbackPage() {
  return (
    <FeaturePlaceholder
      title="Feedback"
      description="This route will host employee feedback review flows and operational follow-up tooling."
      note="Error, empty, and loading states are already standardized so feedback lists can reuse them directly."
    />
  )
}
