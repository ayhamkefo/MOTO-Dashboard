import { FeaturePlaceholder } from '../../../shared/components/feature-placeholder'

export function BrandsPage() {
  return (
    <FeaturePlaceholder
      title="Brands"
      description="This route will host brand CRUD, status handling, and brand-level metadata."
      note="Shared table, card, and state patterns are ready so the brands module can stay thin and predictable."
    />
  )
}
